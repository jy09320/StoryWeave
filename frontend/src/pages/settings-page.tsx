import { EmptyState } from '@/components/empty-state'

export function SettingsPage() {
  return (
    <EmptyState
      title="设置中心待补齐"
      description="这一页已经纳入新的工作台导航。后续会在这里统一承接快捷键、外观偏好、运行时策略和编辑器行为设置。"
    />
  )
}
